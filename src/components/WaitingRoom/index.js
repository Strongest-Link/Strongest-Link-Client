import React from "react";
import { QuizSelect } from "..";

const WaitingRoom = () => {
    //load a new div
    //a div for each connected user
    //must click a ready button
    // when there are the required number ready ups proceed to quiz page
    
}

    

export default WaitingRoom;
